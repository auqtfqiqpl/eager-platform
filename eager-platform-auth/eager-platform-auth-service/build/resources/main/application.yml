server:
  port: 8503
  servlet:
    context-path: /user

spring:
  application:
    name: user-provider
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
logging:
  config: classpath:logback.xml
